services:
  # 1. Сервис для миграций (запускается первым и завершается)
  migrate:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: webnet_migrate_prod
    command: python manage.py migrate --noinput
    volumes:
      - .:/app
      - ./data:/app/data
      - ./tg_key_bot.db:/app/tg_key_bot.db  # Монтируем SQLite БД
    networks:
      - webnet_network_prod
    environment:
      - DJANGO_SETTINGS_MODULE=tg_bot_admin.settings
      - DEBUG=False

  # 2. Основной сервис бота
  telegram-vpn-bot:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: webnet_bot_prod
    restart: always
    depends_on:
      migrate:
        condition: service_completed_successfully
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - ADMIN_IDS=${ADMIN_IDS}
      - SUPPORT_GROUP_ID=${SUPPORT_GROUP_ID}
      - PLATEGA_API_KEY=${PLATEGA_API_KEY}
      - PLATEGA_SHOP_ID=${PLATEGA_SHOP_ID}
      - ANTILOPAY_PUBLIC_KEY=${ANTILOPAY_PUBLIC_KEY}
      - ANTILOPAY_SECRET_KEY=${ANTILOPAY_SECRET_KEY}
      - REMNAWAVE_API_URL=${REMNAWAVE_API_URL}
      - REMNAWAVE_API_KEY=${REMNAWAVE_API_KEY}
      - WEBHOOK_HOST=0.0.0.0
      - WEBHOOK_PORT=8023
      - DJANGO_SETTINGS_MODULE=tg_bot_admin.settings
      - SECRET_KEY=${SECRET_KEY}
      - DEBUG=False
    ports:
      - "8123:8123"   # Django admin
      - "8023:8023"   # Webhook server (тот же порт)
    expose:
      - "8023"
      - "8123"
    volumes:
      - .:/app
      - ./data:/app/data
      - ./tg_key_bot.db:/app/tg_key_bot.db  # Монтируем SQLite БД
      - ./images:/app/images  # Монтируем папку с изображениями (опционально)
      - ./bot_security.log:/app/bot_security.log  # Логи (опционально)
    networks:
      - webnet_network_prod
    command: >
      sh -c "python run_bot_with_django.py"

  # 3. Nginx
  nginx:
    image: nginx:alpine
    container_name: webnet_nginx_prod
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./static:/app/static  # Если есть статика
      - ./media:/app/media    # Если есть медиа
    depends_on:
      - telegram-vpn-bot
    networks:
      - webnet_network_prod

networks:
  webnet_network_prod:
    driver: bridge